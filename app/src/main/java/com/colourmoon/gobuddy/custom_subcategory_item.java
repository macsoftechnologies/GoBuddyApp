package com.colourmoon.gobuddy;

/*import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.LinearLayoutManager;
import androidx.recyclerview.widget.RecyclerView;

import android.os.Bundle;

import java.util.ArrayList;
import java.util.List;

public class custom_subcategory_item extends AppCompatActivity {
    private RecyclerView childRecyclerView;
    private List<String> childItemList = new ArrayList<>(); // In


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
      LinearLayoutManager
                layoutManager
                = new LinearLayoutManager(
                custom_subcategory_item.this);


        childRecyclerView = findViewById(R.id.child_recyclerview);

        // Initialize and set up the ChildAdapter for the childRecyclerView
        ChildAdapter childAdapter = new ChildAdapter(this, childItemList);
        childRecyclerView.setAdapter(childAdapter);
        addChildItems();
    }

  private void addChildItems() {
        childItemList.add("Child Item 1");
        childItemList.add("Child Item 2");
        // Add more items as needed
    }
}*/