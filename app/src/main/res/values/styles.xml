<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/AppTextViewStyle</item>
        <item name="buttonStyle">@style/AppButtonViewStyle</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back_arrow</item>
        <item name="android:actionMenuTextAppearance">@style/MenuTextTheme</item>
        <item name="android:fontFamily">@font/poppins</item>

    </style>

    <style name="MenuTextTheme" parent="AppTheme">
        <item name="android:textSize">@dimen/_14ssp</item>
    </style>
    <!-- No Action bar application theme. -->
    <style name="NoActionBarAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@style/AppTextViewStyle</item>
        <item name="buttonStyle">@style/AppButtonViewStyle</item>
        <item name="android:fontFamily">@font/poppins</item>

    </style>

    <style name="TextInputLayoutHintAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="MyAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    //changing the entire application font style for textviews

    <style name="AppTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    // changing the entire application font style for buttonviews

    <style name="AppButtonViewStyle" parent="android:Widget.Button">
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <style name="GlideSliderCardStyle" parent="Base.CardView">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="cardCornerRadius">@dimen/_10sdp</item>
    </style>

    <style name="MyTabLayoutTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/_18ssp</item>
    </style>
</resources>
